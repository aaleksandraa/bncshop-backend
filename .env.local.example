# Local dev seeder credentials — copy lines into .env for `php artisan db:seed` only.
# Never use in production. Never commit real passwords.

ADMIN_EMAIL=admin@bncshop.test
ADMIN_PASSWORD=
ADMIN_NAME="BNC Admin"
SELLER_EMAIL=prodavac@bncshop.test
SELLER_PASSWORD=
SELLER_NAME="BNC Prodavac"
